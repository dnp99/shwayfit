rules_version = '2';

// ShwayFit POC: all business data is accessed through the Go API using the
// server Firestore client. Browser clients must never access Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
