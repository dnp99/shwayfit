# Frontend design-system history

The active ShwayFit UI specification is [design-system.md](design-system.md). This retained document records the original adoption decision; it is not a second source of truth.

Tailwind CSS, shadcn/ui, semantic CSS tokens, Lucide icons, and System/Light/Dark mode will be introduced with the authenticated application shell. The landing page remains intentionally lightweight until that work begins.
